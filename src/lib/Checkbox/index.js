import React, {Component} from 'react';
import './tiny-checkbox.css';
import PropTypes from 'prop-types';

export default class Checkbox extends Component {

    render() {
        return (
            <span>

            </span>
        );
    }
}